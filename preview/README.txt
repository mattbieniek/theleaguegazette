The main prototype is an Astro project. Run `npm install` then `npm run dev` from the project root.
The source is intentionally organized for GitHub/Vercel deployment rather than as a single static HTML file.
